import { useState } from 'react'
import { X } from 'lucide-react'

export default function RespondDialog({ request, onClose, onSend }) {
  const [response, setResponse] = useState('')
  if (!request) return null
  return <div className="fixed inset-0 z-[80] grid place-items-center bg-slate-950/45 p-4" role="presentation" onMouseDown={event => { if (event.target === event.currentTarget) onClose() }}><div role="dialog" aria-modal="true" aria-labelledby="request-dialog-title" className="w-full max-w-md rounded-xl bg-white p-5 shadow-xl"><div className="flex items-start justify-between gap-3"><div><h2 id="request-dialog-title" className="text-lg font-bold text-[#172342]">Respond to {request.name}</h2><p className="mt-1 text-sm text-slate-600">{request.crop} · {request.issue} · {request.product}</p></div><button type="button" onClick={onClose} aria-label="Close response" className="rounded p-1 hover:bg-slate-100"><X size={20} /></button></div><form onSubmit={event => { event.preventDefault(); if (response.trim()) onSend(request, response.trim()) }} className="mt-4"><label htmlFor="shopkeeper-response" className="text-sm font-semibold text-slate-700">Your response</label><textarea id="shopkeeper-response" autoFocus required value={response} onChange={event => setResponse(event.target.value)} rows={4} placeholder="Recommend a suitable product or ask the farmer for more details..." className="mt-2 w-full resize-y rounded-lg border border-slate-300 p-3 text-sm outline-none focus:border-emerald-600" /><div className="mt-4 flex justify-end gap-2"><button type="button" onClick={onClose} className="rounded-lg border border-slate-300 px-4 py-2 text-sm">Cancel</button><button type="submit" className="rounded-lg bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Send response</button></div></form></div></div>
}
