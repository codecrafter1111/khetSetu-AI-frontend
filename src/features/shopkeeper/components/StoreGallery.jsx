import { useRef } from 'react'
import { ArrowRight, Image, Plus } from 'lucide-react'

export default function StoreGallery({ photos, onAdd, onViewAll }) {
  const inputRef = useRef(null)
  return <section className="min-w-0 rounded-2xl border border-[#dce8ee] bg-white p-5 shadow-sm"><div className="mb-4 flex items-center justify-between gap-2"><h2 className="flex items-center gap-2 text-[17px] font-bold text-[#17213b]"><Image size={19} className="text-emerald-800" />Store Gallery</h2><button type="button" onClick={onViewAll} className="inline-flex items-center gap-1 text-[12px] font-medium text-[#208151] hover:underline">View All <ArrowRight size={15} /></button></div><div className="flex gap-3 overflow-x-auto pb-1">{photos.map(photo => <button key={photo.id} type="button" onClick={onViewAll} aria-label={`View ${photo.alt}`} className="h-[115px] min-w-[145px] flex-1 overflow-hidden rounded-md border border-[#e5edf2] bg-[#f5faf7] sm:min-w-[155px]"><span role="img" aria-label={photo.alt} className="block size-full bg-no-repeat" style={{ backgroundImage: `url('${photo.src}')`, backgroundPosition: photo.position ?? 'center', backgroundSize: photo.size ?? 'cover' }} /></button>)}<button type="button" onClick={() => inputRef.current?.click()} className="flex h-[115px] min-w-[145px] flex-1 flex-col items-center justify-center rounded-md border border-dashed border-[#bed2e5] text-[#334c73] hover:bg-emerald-50 sm:min-w-[155px]"><Plus size={21} /><span className="text-[12px]">Add Photo</span></button></div><input ref={inputRef} type="file" accept="image/*" multiple className="sr-only" onChange={event => { onAdd(event.target.files); event.target.value = '' }} /></section>
}
