import { Clock3, MapPin, Navigation, Phone, Star } from 'lucide-react'
import Card from '../../../../components/ui/Card'

export default function StoreContact({ store }) {
  if (!store) return <Card className="min-w-0 p-4"><h2 className="text-base font-bold">Store Contact</h2><p className="mt-4 text-sm text-slate-500">Select a store to see its contact details.</p></Card>
  const mapsUrl = `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`${store.name}, ${store.address}, Uttar Pradesh`)}`
  return <Card className="min-w-0 p-4 xl:min-h-[310px] xl:flex xl:flex-col xl:justify-between"><h2 className="flex items-center gap-2 text-base font-bold text-slate-900"><Phone className="size-5 fill-emerald-700 text-emerald-700" />Store Contact</h2><div className="mt-3 flex items-center gap-3"><img src="/images/agri-stores/storefront.png" alt="Local agricultural supply store" className="size-16 shrink-0 rounded-lg object-cover" /><div className="min-w-0"><h3 className="truncate text-sm font-bold text-slate-900">{store.name}</h3><p className="text-xs text-slate-600"><Star className="inline size-4 fill-amber-400 text-amber-400" /> <strong className="text-slate-900">{store.rating.toFixed(1)}</strong> ({store.reviews} reviews)</p><p className="text-xs text-slate-500">Your Trusted Farming Partner</p></div></div><div className="mt-3 space-y-2 text-[11px]"><p className="flex items-start gap-1.5 text-slate-600"><MapPin className="size-4 shrink-0" /><span>{store.address} - {store.pin}<br />{store.distance} km from your location</span></p><div className="flex items-center justify-between gap-2"><p className="flex items-center gap-1.5 text-slate-600"><Clock3 className="size-4 shrink-0" /><strong className={store.open ? 'text-emerald-800' : 'text-red-700'}>{store.open ? 'Open Now' : 'Closed Now'}</strong><span>(8:00 AM – 8:00 PM)</span></p><span className={`rounded-full px-2 py-0.5 ${store.open ? 'bg-emerald-100 text-emerald-800' : 'bg-red-100 text-red-700'}`}>{store.open ? 'Open' : 'Closed'}</span></div></div><div className="mt-3 grid grid-cols-2 gap-2"><button type="button" className="flex h-9 items-center justify-center gap-2 rounded-lg bg-emerald-700 text-xs font-medium text-white hover:bg-emerald-800"><Phone className="size-4" />Call Now</button><a href={mapsUrl} target="_blank" rel="noopener noreferrer" className="flex h-9 items-center justify-center gap-2 rounded-lg border border-emerald-700 text-xs font-medium text-emerald-800 hover:bg-emerald-50"><Navigation className="size-4" />Get Directions</a></div></Card>
}
