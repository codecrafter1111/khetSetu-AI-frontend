import { useState } from 'react'
import { ArrowRight, Map, MapPin, Minus, Plus } from 'lucide-react'
import Card from '../../../../components/ui/Card'

export default function StoresMap({ stores, selectedId, onSelect }) {
  const [zoom, setZoom] = useState(1)
  return <Card className="min-w-0 p-3 xl:min-h-[425px]"><div className="flex items-center justify-between gap-2 px-1"><h2 className="flex items-center gap-2 text-base font-bold text-slate-900"><Map className="size-6 fill-emerald-700 text-emerald-700" />Stores Near You</h2><span className="inline-flex items-center gap-1 text-[11px] font-medium text-emerald-800">View on Full Map <ArrowRight className="size-4" /></span></div><div className="relative mt-2 h-[365px] overflow-hidden rounded-xl bg-slate-100"><div className="absolute inset-0 bg-[url('/images/agri-stores/varanasi-map.png')] bg-cover bg-center transition-transform" style={{ transform: `scale(${zoom})` }} /><span className="absolute left-[43%] top-[37%] rounded bg-white/75 px-1 text-base font-medium text-slate-800">Varanasi</span><span className="absolute left-[48%] top-[63%] rounded bg-white/65 px-1 text-[10px] text-slate-700">BHELUPUR</span><span className="absolute left-[19%] top-[78%] rounded bg-white/65 px-1 text-[10px] text-slate-700">SIGRA</span><span className="absolute right-[10%] top-[38%] rotate-90 text-[10px] text-sky-700">Ganga</span><div className="absolute left-2 top-2 rounded-lg bg-white/95 px-2 py-1 text-[10px] shadow"><p className="flex items-center gap-1"><MapPin className="size-3 fill-emerald-700 text-emerald-700" />Store Location</p><p className="mt-0.5 flex items-center gap-1"><span className="size-2.5 rounded-full bg-blue-600" />Your Location</p></div>{stores.map(store => <button key={store.id} type="button" onClick={() => onSelect(store.id)} title={store.name} aria-label={`Select ${store.name} on map`} className={`absolute grid size-7 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full border-2 border-white text-xs font-bold text-white shadow-md ${selectedId === store.id ? 'bg-emerald-900 ring-4 ring-emerald-300/70' : 'bg-emerald-700'}`} style={store.position}>{store.id}</button>)}<span className="absolute left-[44%] top-[48%] size-5 rounded-full border-[5px] border-white bg-blue-600 shadow-[0_0_0_8px_rgba(59,130,246,.15)]" aria-label="Your location" /><div className="absolute bottom-4 right-3 grid overflow-hidden rounded-lg bg-white shadow"><button type="button" aria-label="Zoom in" onClick={() => setZoom(value => Math.min(1.4, +(value + 0.1).toFixed(1)))} className="grid size-8 place-items-center border-b border-slate-200"><Plus className="size-4" /></button><button type="button" aria-label="Zoom out" onClick={() => setZoom(value => Math.max(1, +(value - 0.1).toFixed(1)))} className="grid size-8 place-items-center"><Minus className="size-4" /></button></div></div></Card>
}
