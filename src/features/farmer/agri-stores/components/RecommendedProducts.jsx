import { ArrowRight, Check, ShoppingCart } from 'lucide-react'
import Card from '../../../../components/ui/Card'

export default function RecommendedProducts({ products }) {
  return <Card className="min-w-0 p-2.5 xl:min-h-[310px] xl:flex xl:flex-col xl:justify-between"><div className="flex items-center justify-between gap-2 px-1"><h2 className="flex items-center gap-2 text-base font-bold text-slate-900"><ShoppingCart className="size-6 fill-emerald-700 text-emerald-700" />Recommended Products</h2><span className="inline-flex items-center gap-1 text-[11px] font-medium text-emerald-800">View All Products <ArrowRight className="size-4" /></span></div><div className="mt-2 flex gap-2 overflow-x-auto pb-1">{products.length ? products.map(product => <article key={product.id} className="min-w-[112px] flex-1 rounded-xl border border-slate-200 p-2"><div role="img" aria-label={product.name} className="mx-auto h-[115px] w-full bg-[url('/images/agri-stores/input-products.png')] bg-[length:400%_100%] bg-no-repeat" style={{ backgroundPosition: product.position }} /><h3 className="mt-1 min-h-7 text-[11px] font-semibold leading-tight text-slate-900">{product.name}</h3><p className="text-[10px] text-slate-500">{product.category}</p><p className="mt-1 text-[11px] font-bold text-slate-900">{product.price} <span className="font-normal text-slate-600">/ {product.unit}</span></p><span className="mt-1 inline-flex items-center gap-1 rounded-full bg-emerald-100 px-2 py-0.5 text-[10px] text-emerald-800"><Check className="size-3" />In Stock</span></article>) : <p className="p-4 text-xs text-slate-500">No recommended products match this filter.</p>}</div></Card>
}
