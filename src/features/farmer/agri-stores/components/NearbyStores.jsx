import { ChevronRight, MapPin, Star } from 'lucide-react'
import Card from '../../../../components/ui/Card'

export default function NearbyStores({ stores, totalCount, selectedId, onSelect, sort, onSort }) {
  return <Card className={`min-w-0 p-3 ${stores.length >= 5 ? 'xl:min-h-[425px]' : ''}`}><div className="flex items-center justify-between gap-2"><h2 className="text-base font-bold text-slate-900">Nearby Stores ({totalCount})</h2><select aria-label="Sort stores" value={sort} onChange={event => onSort(event.target.value)} className="min-w-0 rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-[11px] text-slate-700"><option value="distance">Sort by Distance</option><option value="rating">Sort by Rating</option></select></div><div className="mt-2">{stores.length ? stores.map(store => <button key={store.id} type="button" onClick={() => onSelect(store.id)} className={`flex min-h-[70px] w-full items-center gap-2 border-b border-slate-200 px-1 py-1 text-left last:border-b-0 ${selectedId === store.id ? 'bg-emerald-50/50' : 'hover:bg-slate-50'}`}><span className="grid size-7 shrink-0 place-items-center rounded-full bg-emerald-700 text-xs font-bold text-white">{store.id}</span><span className="min-w-0 flex-1"><strong className="block truncate text-[11px] text-slate-900">{store.name}</strong><span className="block truncate text-[10px] text-slate-500">{store.address}</span></span><span className="flex shrink-0 flex-col items-end gap-1"><span className="whitespace-nowrap text-[10px]"><Star className="inline size-3 fill-amber-400 text-amber-400" /> <strong>{store.rating.toFixed(1)}</strong> <span className="text-slate-500">({store.reviews})</span></span><span className="flex items-center gap-0.5 text-[10px] text-slate-500"><MapPin className="size-3" />{store.distance} km</span><span className={`rounded-full px-2 py-0.5 text-[10px] ${store.open ? 'bg-emerald-100 text-emerald-800' : 'bg-red-100 text-red-700'}`}>{store.open ? 'Open' : 'Closed'}</span></span><ChevronRight className="size-4 shrink-0 text-slate-500" /></button>) : <p className="rounded-lg bg-slate-50 p-4 text-sm text-slate-600">No stores match these filters. Try a wider distance or another product.</p>}</div></Card>
}
