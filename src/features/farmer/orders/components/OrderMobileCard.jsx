import { EllipsisVertical } from 'lucide-react'
import ProductImage from '../../products/components/ProductImage'
import ConsumerAvatar from './ConsumerAvatar'
import OrderStatusBadge from './OrderStatusBadge'

export default function OrderMobileCard({ order, onView }) {
  return <article className="rounded-xl border border-slate-200 p-3"><div className="flex items-center justify-between gap-2"><strong className="text-sm text-slate-900">#{order.id}</strong><span className="text-xs text-slate-500">{order.dateLabel}</span></div><div className="mt-3 flex items-center gap-2"><ConsumerAvatar consumer={order.consumer} className="size-9" /><div><p className="text-xs font-semibold text-slate-900">{order.consumer.name}</p><p className="text-[11px] text-slate-500">{order.consumer.location}</p></div></div><div className="mt-3 flex items-center gap-2"><ProductImage product={order.product} className="size-10" /><div className="min-w-0 flex-1"><p className="text-xs font-semibold text-slate-900">{order.product.name}</p><p className="text-[11px] text-slate-500">{order.quantity}</p></div><strong className="text-sm text-slate-900">₹ {order.amount.toLocaleString('en-IN')}</strong></div><div className="mt-3 flex items-center justify-between gap-2"><div className="flex flex-wrap gap-1.5"><OrderStatusBadge status={order.deliveryStatus} /><OrderStatusBadge status={order.paymentStatus} type="payment" /></div><div className="flex items-center gap-1"><button type="button" onClick={() => onView(order.id)} className="rounded-lg border border-slate-200 px-3 py-1.5 text-xs font-semibold text-slate-700">View</button><button type="button" aria-label={`More actions for order ${order.id}`} className="rounded-lg p-1.5"><EllipsisVertical className="size-4" /></button></div></div></article>
}
