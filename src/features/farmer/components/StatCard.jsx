import { TrendingUp } from 'lucide-react'
import Card from '../../../components/ui/Card'
export default function StatCard({ stat }) { const Icon=stat.icon; const amber=stat.accent==='amber'; return <Card className="flex min-h-[135px] items-center gap-4 px-4 py-3"><div className={`grid size-[58px] shrink-0 place-items-center rounded-2xl text-white shadow-sm ${amber?'bg-amber-400':'bg-gradient-to-br from-emerald-600 to-emerald-800'}`}><Icon className={`size-7 ${amber?'fill-white':''}`}/></div><div className="min-w-0"><p className="text-sm text-slate-600">{stat.label}</p><div className="mt-0.5 flex flex-wrap items-center gap-2"><strong className="text-[27px] leading-tight font-black text-slate-950">{stat.value}</strong><span className="flex items-center gap-0.5 rounded-full bg-emerald-100 px-2 py-1 text-xs font-bold text-emerald-700"><TrendingUp className="size-3"/>{stat.change}</span></div><p className="mt-1 text-xs text-slate-500">{stat.detail}</p></div></Card> }
