import { ChevronRight, Star } from 'lucide-react'; import Card from '../../../components/ui/Card'; import SectionHeader from '../../../components/common/SectionHeader'; import { keyFeatures } from '../data/dashboard.mock'
export default function KeyFeatures(){return <Card className="bg-gradient-to-br from-white to-emerald-50 p-4"><SectionHeader icon={Star} title="Key Features"/>{keyFeatures.map(({icon:Icon,title,text,color})=><button key={title} className="flex w-full items-center gap-3 border-b border-emerald-100 py-3 text-left last:border-0"><Icon className={`size-6 shrink-0 ${color||'text-emerald-700'}`}/><span className="min-w-0"><strong className="block text-xs text-slate-800">{title}</strong><span className="block truncate text-[10px] text-slate-500">{text}</span></span><ChevronRight className="ml-auto size-4 shrink-0 text-slate-500"/></button>)}</Card>}
